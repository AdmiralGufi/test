import {NextResponse} from 'next/server';import {sql} from '../../../lib/db';import {createSession} from '../../../lib/auth';
export async function POST(r){const {name,email,password}=await r.json();if(!name||!email||!password||password.length<8)return NextResponse.json({error:'INVALID_INPUT'},{status:400});const c=await sql`select count(*)::int n from users`;if(c[0].n>0)return NextResponse.json({error:'ALREADY_CONFIGURED'},{status:409});const u=await sql`insert into users(email,name,password_hash,role,active) values(${email.toLowerCase()},${name},crypt(${password},gen_salt('bf')),'ADMIN',true) returning id,email,name,role`;await createSession(u[0].id);return NextResponse.json({user:u[0]});}
