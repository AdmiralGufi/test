import {NextResponse} from 'next/server';import {sql} from '../../../lib/db';import {getCurrentUser} from '../../../lib/auth';
export async function POST(r){const u=await getCurrentUser();if(!u)return NextResponse.json({error:'UNAUTHORIZED'},{status:401});const x=await r.json();const code=String(x.device_code||'').trim();if(!code)return NextResponse.json({error:'DEVICE_CODE_REQUIRED'},{status:400});const rows=await sql`insert into devices(device_code,device_type,user_id,platform,label,user_agent,active,last_seen_at,paired_at) values(${code},${x.device_type||'PHONE'},${u.id},${x.platform||null},${x.label||null},${x.user_agent||null},true,now(),now()) on conflict(device_code) do update set device_type=excluded.device_type,user_id=excluded.user_id,platform=excluded.platform,label=excluded.label,user_agent=excluded.user_agent,active=true,last_seen_at=now() returning *`;return NextResponse.json(rows[0]);}
